<% jjaek_policy = policy(jjaek) %>
<% content_visible = jjaek_policy.view_original_content? %>
<% inline_comments_context = local_assigns[:inline_comments_context] %>
<% inline_comments_profile_user = local_assigns[:inline_comments_profile_user] %>
<% inline_comments_book = local_assigns[:inline_comments_book] %>
<% detail_context = local_assigns.fetch(:detail_context, false) %>

<% wrapper_class = if jjaek.hidden?
  "rounded-3xl border border-red-200 bg-red-50/60 p-5 text-red-900 shadow-sm"
else
  "rounded-3xl border border-stone-200 bg-white p-5 shadow-sm"
end %>

<article id="<%= dom_id(jjaek) %>" class="<%= wrapper_class.squish %>">
  <% if jjaek.hidden? %>
    <%= render "jjaeks/hidden_placeholder", jjaek: jjaek %>
  <% end %>

  <% if content_visible %>
    <div class="<%= "mt-4 rounded-2xl border border-red-100 bg-white p-5 text-stone-900" if jjaek.hidden? %>">
      <%= render "jjaeks/jjaek_header", jjaek: jjaek %>
      <%= render "jjaeks/jjaek_body", jjaek: jjaek %>

      <% unless jjaek.hidden? %>
        <%= render "jjaeks/jjaek_actions",
                   jjaek: jjaek,
                   inline_comments_context: inline_comments_context,
                   inline_comments_profile_user: inline_comments_profile_user,
                   inline_comments_book: inline_comments_book,
                   detail_context: detail_context %>
      <% end %>

      <% if inline_comments_context.present? %>
        <div id="<%= comments_panel_dom_id(jjaek, context: inline_comments_context, profile_user: inline_comments_profile_user, book: inline_comments_book) %>"></div>
      <% end %>
    </div>
  <% end %>

  <% if jjaek_policy.view_hidden_read_actions? && (inline_comments_context.present? || detail_context) %>
    <%= render "jjaeks/hidden_read_actions",
               jjaek: jjaek,
               inline_comments_context: detail_context ? :detail : inline_comments_context,
               inline_comments_profile_user: inline_comments_profile_user,
               inline_comments_book: inline_comments_book %>
    <% if inline_comments_context.present? && !content_visible %>
      <div id="<%= comments_panel_dom_id(jjaek, context: inline_comments_context, profile_user: inline_comments_profile_user, book: inline_comments_book) %>"></div>
    <% end %>
  <% elsif jjaek.hidden? && inline_comments_context == :group && jjaek_policy.show? %>
    <div class="mt-4 text-xs">
      <%= link_to t("jjaeks.actions.view"), jjaek_path(jjaek), class: "font-medium text-red-800" %>
    </div>
  <% end %>
</article>
