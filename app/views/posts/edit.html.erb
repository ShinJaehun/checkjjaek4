<section class="mx-auto w-full max-w-2xl rounded-[2rem] border border-stone-200 bg-white p-6 shadow-sm">
  <h1 class="text-2xl font-semibold tracking-tight text-stone-900"><%= t("posts.edit.title") %></h1>
  <div class="mt-6">
    <%= render "form", post: @post, submit_label: t("posts.actions.save") %>
  </div>
</section>
