<% if bookshelf_entry.present? %>
  <section class="rounded-[2rem] border border-stone-200 bg-white p-6 shadow-sm">
    <h2 class="text-lg font-semibold text-stone-900"><%= t("books.show.state_title") %></h2>
    <p class="mt-1 text-sm text-stone-500"><%= t("books.show.state_description") %></p>

    <div class="mt-5">
      <%= render "bookshelf_entries/form",
                 bookshelf_entry:,
                 book:,
                 sticker_definitions:,
                 submit_label: t("books.show.save_state") %>
    </div>
  </section>
<% else %>
  <section class="rounded-[2rem] border border-dashed border-stone-300 bg-white p-6 text-sm text-stone-500 shadow-sm">
    <p><%= t("books.show.read_only_description") %></p>
    <div class="mt-4">
      <%= button_to t("bookshelf_entries.new.title"),
                    bookshelf_entries_path,
                    method: :post,
                    params: {
                      book_id: book.id,
                      bookshelf_entry: { status: :wish }
                    },
                    class: "rounded-full bg-stone-900 px-4 py-2 text-sm font-medium text-stone-50" %>
    </div>
  </section>
<% end %>
